======================================================== MATLAB → MathLab (Spyder/IPython) Symbolic via SymPy | Numeric via NumPy | SciPy ODE========================================================--- CONSOLE / WORKSPACE ---MATLAB          →  MathLab--------------------------------------------------------clc             →  clear()               (console only)clear           →  %reset -f             (wipes all vars)clear x y       →  del x, ywho             →  %whowhos            →  %whoshelp foo        →  help(foo)doc foo         →  foo?exit            →  exit()  or  Ctrl-DNOTE: workspace auto-saves on exit and restores on open.      To wipe saved state: workspace.clear_workspace()--- FILES / DIRECTORIES ---MATLAB          →  Python--------------------------------------------------------pwd             →  os.getcwd()cd folder       →  os.chdir("folder")ls              →  !lsmkdir folder    →  os.mkdir("folder")--- RUNNING CODE ---MATLAB                    →  Spyder--------------------------------------------------------Run script                →  F5Run selection             →  Highlight + F9Run cell                  →  # %%  markers + Ctrl-EnterRun in base workspace     →  Run in current console--- SYMBOLIC MATH ---MATLAB (Symbolic TB)      →  MathLab--------------------------------------------------------syms x y z                →  x,y,z pre-declareddiff(f, x)                →  diff(f, x)diff(f, x, 2)             →  diff(f, x, 2)int(f, x)                 →  integ(f, x)int(f, x, a, b)           →  integ(f, x, a, b)limit(f, x, a)            →  lim(f, x, a)taylor(f, x, n)           →  ser(f, x, 0, n)solve(f, x)               →  ssolve(f, x)solve([f,g], [x,y])       →  ssolve([f,g], [x,y])simplify(f)               →  simplify(f)factor(f)                 →  factor(f)expand(f)                 →  expand(f)pretty(f)                 →  pretty(f)--- VECTOR CALCULUS (symbolic) ---MATLAB                    →  MathLab--------------------------------------------------------gradient(f, [x,y,z])      →  grad(f)divergence(F, [x,y,z])    →  div(F)curl(F, [x,y,z])          →  curl(F)del^2 f                   →  laplacian(f)[Fx;Fy;Fz]                →  vec(Fx, Fy, Fz)--- LINEAR ALGEBRA (numeric) ---MATLAB          →  MathLab--------------------------------------------------------zeros(n,m)      →  zeros((n,m))ones(n,m)       →  ones((n,m))eye(n)          →  eye(n)linspace(a,b,n) →  linspace(a,b,n)diag(v)         →  diag(v)A'              →  trans(A)  or  A.TA*B             →  A @ B             (matrix multiply)A.*B            →  A * B             (element-wise)inv(A)          →  inv(A)det(A)          →  det(A)[V,D]=eig(A)    →  vals,vecs = eig(A)A\b             →  lsolve(A, b)rank(A)         →  rank(A)norm(v)         →  norm(v)null(A)         →  null(A)trace(A)        →  tr(A)pinv(A)         →  pinv(A)LINEAR ALGEBRA (symbolic matrix):A = M([[1,x],[x,1]])A.det()  A.inv()  A.eigenvals()  A.rref()--- POLYNOMIALS ---MATLAB          →  MathLab--------------------------------------------------------roots(p)        →  roots(p)polyval(p,x)    →  polyval(p,x)polyfit(x,y,n)  →  polyfit(x,y,n)--- NUMERICAL HELPERS ---MATLAB          →  MathLab--------------------------------------------------------interp1(x,y,xq) →  interp1(xdata, ydata, xq)fzero(f,[a,b])  →  fzero(f, [a, b])ode45(f,t,y0)   →  ode45(f, tspan, y0)fplot(f,[a,b])  →  fplot(expr, x, a, b)--- PLOTTING ---MATLAB          →  Python--------------------------------------------------------plot(x,y)       →  plt.plot(x,y)hold on         →  call plt.plot() again (implicit hold)grid on         →  plt.grid(True)legend(...)     →  plt.legend()title(...)      →  plt.title("...")xlabel(...)     →  plt.xlabel("...")ylabel(...)     →  plt.ylabel("...")close           →  plt.close()close all       →  plt.close("all")semilogy        →  plt.semilogy()semilogx        →  plt.semilogx()loglog          →  plt.loglog()subplot(m,n,k)  →  plt.subplot(m,n,k)figure          →  plt.figure()Quick plot template:  x = linspace(0, 2*np.pi, 500)  plt.plot(x, np.sin(x), label="sin")  plt.plot(x, np.cos(x), label="cos")  plt.legend()  plt.show()--- PHYSICS CONSTANTS (packs/physics) ---c       speed of light       299_792_458  m/sqe      elementary charge    1.602e-19    Cme      electron mass        9.109e-31    kgmp      proton mass          1.673e-27    kgeps0    permittivity         8.854e-12    F/mmu0     permeability         4π×10⁻⁷     H/mh       Planck               6.626e-34    J·skB      Boltzmann            1.381e-23    J/KUnits: Hz kHz MHz GHz | eV keV MeV GeV       nm um mm cm | ps ns us ms       uT mT T Gauss | deg rad--- PACKS (load when needed) ---from packs.rf   import *   →  rf_help()from packs.beam import *   →  beam_help()RF:   dBm_to_W, W_to_dBm, vswr, gamma_from_Z,      return_loss, freq_to_wl, skin_depth,      thermal_noise_dBmBEAM: beta_from_eV, gamma_from_eV, rigidity,      larmor_radius, R50_to_energy,      HDPE_to_water_depth, find_R50, find_Rp--- IPYTHON EXTRAS ---%who        list variables (short)%whos       list variables (detailed)%time expr  time an expression%reset -f   wipe workspace!cmd        run shell command--- HELP ---cheatsheet()    this file in the consoleexamples()      working code examplesfoo?            docstring for any function========================================================